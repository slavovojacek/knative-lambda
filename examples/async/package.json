{
  "name": "async",
  "version": "0.0.0",
  "description": "Async serving example",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --minify --sourcemap=external --platform=node --outfile=build/main.js"
  },
  "dependencies": {
    "knative-lambda": "0.0.x"
  },
  "devDependencies": {
    "@slavovojacek/tsconfig": "0.0.x",
    "@types/node": "16.x",
    "esbuild": "0.14.x",
    "typescript": "4.5.x"
  }
}
